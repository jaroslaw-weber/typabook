import { Canvas, Meta} from '@storybook/blocks';

import * as Text from './Text.stories';


<Meta of={Text} />

# Navbar

Text that will be typed. Current letter is highlighted.

<Canvas of={Text.Default} />